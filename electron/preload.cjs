// Reserved for future secure IPC bridges between the renderer and main process.
// The app currently talks to its bundled local server over plain HTTP (localhost),
// so no privileged bridge is needed yet.
